// Barrel único para o client gerado pelo Prisma 7 (provider "prisma-client", saída em
// src/generated/prisma — dentro de src/ de propósito, ver schema.prisma). Todo o resto
// do app importa daqui, não do caminho gerado diretamente — se o Prisma mudar o output
// de novo, só este arquivo muda.
export * from '../../generated/prisma/client';
